# Excel MCP 开发方案

桌面项目：`C:\Users\limaohua\Desktop\excel-mcp`。公开仓库和安装包使用 `excel-sovereign-mcp`，避免和已有的 `excel-mcp-server` 抢导入名。

两套引擎，三个工具。模型不选择引擎，也不持有 Excel 会话。一次调用里的全部操作共用一份 `ops`，由服务端决定整批走 openpyxl 还是 COM。保存成功之后才截图；截图失败不回滚已经落盘的修改。

## 结论

openpyxl 能把行和列在文件里挪走，但[官方说明](https://openpyxl.readthedocs.io/en/stable/editing_worksheets.html)写明：它不维护公式、表格、图表这些依赖。`move_range(..., translate=True)` 只翻译被挪动区域内的公式。`vendor/excel-mcp-server/src/excel_mcp/sheet.py` 的 `insert_rows` 就是 `worksheet.insert_rows`，`rename_sheet` 只赋值 `sheet.title`。单元格里的 `=旧表!A1` 不会改写。

[knorq-ai/xlsx-mcp-server](https://github.com/knorq-ai/xlsx-mcp-server) 用 ExcelJS，在 README 的 Known limitations 里写了同一条：插删行列不平移公式引用，并且不算公式。`src/engine/sheets.ts` 的重命名只改 `ws.name`，复制工作表是逐格抄值和样式。这条路不补。

因此：

| 工作 | 引擎 |
|---|---|
| 读区域、写值、写公式、LAMBDA 定义名称 | 纯包走 openpyxl；包内已有 COM 部件则整批改走 COM |
| 字体、字号、字色、填充、边框、对齐、数字格式、行高列宽、合并 | 同上 |
| 新建工作表；纯 xlsm 保存时保留 `vbaProject.bin` | openpyxl，且 `load_workbook(..., keep_vba=True)` |
| 重命名、复制、移动、删除工作表 | COM。Excel 会改写公式和定义名称；openpyxl 不会 |
| 插入或删除行、列、单元格 | COM（`EntireRow` / `EntireColumn`） |
| 与 Excel 表相交的写入、表内加行 | COM 的 table 命令，不用区域格式去刷表体 |
| 重算，把缓存值写回 | COM，`Application.Calculate()` |
| 列出、改写、运行 VBA | COM |
| Excel 表、透视表、图表、切片器、Power Query、数据模型、DAX | COM |
| 每次写操作之后的截图 | COM，同一次调用的末尾 |

纯包指 zip 里没有下面这些部件：`xl/pivotCaches/`、`xl/pivotTables/`、`xl/queryTables/`、`xl/connections.xml`、`xl/model/`、`customXml/`、`xl/slicers/`、`xl/slicerCaches/`、`xl/charts/`、`xl/metadata.xml`、`xl/richData/`、`xl/ctrlProps/`、`xl/activeX/`、`xl/embeddings/`。有其中任意一个，这次调用即使只改字色，也整批走 COM。openpyxl 保存会按自己的对象模型重写整个包，没加载的部件会丢。`vendor/excel-mcp-server` 的 `load_workbook` 都没有 `keep_vba=True`，不能直接拿来存 xlsm。

只有 `vbaProject.bin`、工作表、样式、定义名称时，值、公式和排版仍可以走 openpyxl。`xl/tables/` 单独存在时不因此整批升到 COM；一旦本批范围与某张表的 `ref` 相交，或本批在该表所在工作表上做插删行列，整批走 COM。

文件只接受 `.xlsx` 和 `.xlsm`。`.xls`、`.xlsb`、加密工作簿、IRM 在修改前拒绝。sbroenne 对 IRM 是只读打开，不能当写入路径。

公式写入用英文公式和 `Formula2`（sbroenne `RangeCommands.Formulas.cs` 在支持时写 `Range.Formula2`）。不走 `FormulaLocal`。

## 其他仓库怎么处理这些问题

不克隆 knorq、negokaz。下面是对照之后采纳或不采纳的点。

| 问题 | 别人的做法 | 本方案 |
|---|---|---|
| 一次改很多格 | knorq 的 `write_cells`、`format_cells_bulk` 在一次读写里做完。haris-musa 每个工具各自 `load_workbook` 再 `save` | 一次调用、一次加载、一次保存 |
| 插删行列后公式还指哪 | knorq、haris-musa 都只挪单元格，README 或代码都承认引用不跟着走。sbroenne 用 `EntireRow.Insert()` / `EntireColumn.Insert()` | 结构操作只走 COM |
| 重命名工作表 | knorq `ws.name =`，haris-musa `sheet.title =`。COM 的 `sheet.Name =` 由 Excel 改写引用（`SheetCommands.Lifecycle.cs`） | 重命名、复制、移动、删除工作表走 COM |
| 假透视表 | haris-musa `pivot.py` 用普通 Excel 表加汇总冒充透视表，没有 PivotCache | 不接这段代码 |
| 工作簿里已有透视表、查询、数据模型 | knorq 声明不支持透视表，并称 ExcelJS 保存能留下图表。openpyxl 做不到同等保留 | 写前扫描 zip 部件。有 COM 部件则整批走 COM，避免「只改字色」把部件写丢 |
| 重算 | knorq 明确不算，缓存要用户自己用 Excel 打开才会更新。sbroenne 有 `calculation calculate`，但是单独命令，写公式时还会把计算模式临时改成手动（`xlCalculationManual = -4135`） | 服务端在保存前自己调用工作簿级 `Calculate()`。模型不另调一次 |
| 截图由谁发起 | sbroenne 的 `skills/shared/screenshot.md` 要求模型在建图之后记得再调 `screenshot`。negokaz 的 `excel_screen_capture` 也是独立工具 | 截图写在写操作末尾，不注册成工具 |
| 截图范围 | sbroenne 让模型传入包含图表锚点的范围，并写明不要用已用区域去猜 | 服务端算范围：写入区、溢出区、被改表上的图表和透视表锚点、以及引用了被改表的其他表。超限则 `verified: false` |
| 会话 | sbroenne 把 `session_id` 交给模型，31 个 MCP 工具各自调用。batch 默认出错继续（`--stop-on-error` 才停下） | 会话只在服务端。batch 固定 `--stop-on-error`。失败且尚未保存则关闭时不保存 |
| 文件被用户打开 | sbroenne `FileAccessValidator.ValidateFileNotLocked` 用 `FileShare.None` 探测，拒绝后再说去关掉文件 | 加锁之后、改动之前做同样的独占探测。失败则 `committed: false`，文件不动 |
| 附着到用户正在用的 Excel | sbroenne 用 `Activator.CreateInstance` 另起进程，PID 认不出时禁止 Force-kill | 沿用 excelcli 的独立进程。不 `GetObject` 附着。超时只结束这次认到的 PID |
| 宏在打开时运行 | `ExcelBatch.cs` 对任意 xlsm 把 `AutomationSecurity` 设为 Low（1），为的是同一次 batch 里还能 `vba.run` | 本批含 VBA 读写或运行才用 Low。核对、重算、普通编辑用 ForceDisable（3） |
| 锁 | knorq `src/engine/file-lock.ts` 是进程内 Promise 链，注释写明读不加锁，键用 `path.resolve`。管不住第二个进程，也管不住 COM 还开着文件 | 自写跨进程锁。读和写都拿。心跳在 COM 排队期间继续跳 |
| 读太多 | negokaz 默认单次 4000 格，用 `EXCEL_MCP_PAGING_CELLS_LIMIT`。knorq 单次拒绝超过 10 万格 | 读默认 4000，带下一窗范围。单次写入拒绝超过 10 万格 |
| 对话框挂死 | `ExcelShutdownService` 写明没有总超时，只对 COM busy 做有限次重试 | 每次 COM 调用有总超时。超时后按「已保存 / 未保存」返回，不把队列一直占着 |

## 一次写操作的顺序

模型只调用一次 `workbook_apply` 或 `excel_exec`。两边的 `ops` 是同一份结构。`workbook_apply` 里若出现 COM 才有的动作，服务端整批升到 COM，不要求模型换工具。

```text
1. 按规范化绝对路径加跨进程锁
2. FileShare.None 探测工作簿。被占用则返回 file_locked，不修改
3. 扫描 zip 部件，结合 ops 决定整批走 A 还是 B
4. 执行
     A  在 openpyxl 对象里改完。中途异常不保存
     B  一个 excelcli 会话里按 ops 顺序执行，--stop-on-error
        任一步失败：session close 不保存，committed=false
5. 需要重算时，在尚未释放的会话里 Calculate()
     A 此时才打开 excelcli；B 接着用原会话
6. 保存
     A 临时文件后原子替换
     B session close --save
     保存成功才把 committed 设为 true
7. 截图。失败不回滚。verified=false，ok=false
8. 确认工作簿已从本次 Excel 进程关闭，再释放锁
9. 返回 JSON 和图片
```

A 在第 6 步已经落盘。第 5 步的重算若发生在保存之前，缓存会随这次保存写进文件。因此 A 的实际顺序是：改对象、按规则重算（要重算就先开 COM 会话载入这份尚未落盘的临时文件）、保存、再截图。不要先原子替换再重算后又忘记第二次保存；缓存必须在 `calculated: true` 的那次保存里。

B 从第 4 步就在会话里，截图完成后再关闭。

excelcli 的后台进程可以留着。两次调用之间工作簿必须关掉，否则下一步 openpyxl 打不开文件。

不同文件的 A 路径写入可以并行。任何 COM 步骤（含截图和重算）进全局唯一队列。调用在截图结束后才返回，所以并行只重叠「还没碰到 Excel」的那一段。本机只有一个 Excel，核对是串行的。

锁的顺序固定为：先路径锁，再进 COM 队列。COM 回调里不再去拿另一把路径锁。

只读的 `workbook_read` 拿同一把路径锁，不截图。这样不会读到替换到一半的文件，也不会读到已写入但缓存还没写回的中间态。

### 重算

| 本批改动 | 工作簿里有公式或定义名称 | 行为 |
|---|---|---|
| 只有字体、填充、边框、对齐、数字格式、行高列宽、合并 | 任意 | 不重算。`calc: "skipped"` |
| 值、公式、名称、工作表结构、行列结构 | 有，或本批新写了公式/名称 | `Application.Calculate()`，成功且随后保存成功则 `calc: "done"`、`calculated: true` |
| 同上 | 没有 | 不重算。`calc: "skipped"` |
| 需要重算但失败或超时 |  | `calc: "failed"`，`calculated: false`，`verified: false` |

判断「有公式」：zip 里有 `xl/calcChain.xml`，或工作簿有定义名称，或本批写入了公式。不靠截图里的数字反推。

`calculated: true` 只表示这次已经把重算后的缓存写进文件。跳过重算时它是 false，但只要截图范围完整，`verified` 仍可以为 true。

### 截图范围

每个被改工作表一张图，外接矩形取下面的并集：

- 本批写入范围。结构操作之后用新坐标，不用插行前的地址。
- 公式锚点的溢出区。excelcli 没有现成的溢出命令；`verify.py` 在重算后读 `Range.HasSpill` / `SpillingToRange`。当前 Excel 没有这两个属性时，该格标 `spillUnknown: true`，`verified: false`。
- 这张表上，锚点落在上述范围内的图表和透视表。不用已用区域。
- 其他工作表里，公式文本引用了被改工作表名字的单元格。并成每表一个外接矩形。超过约 40 行 × 16 列就不再整表追，记入 `omitted`。

单张图先按 excelcli 的窗口拼接（`CapturePlanner` 最多 64 块）。仍超过约 1.5 MB 则降低质量后再裁左上，`cropped: true`。只要存在 `omitted`、`spillUnknown` 或 `cropped`，`verified` 就是 false。图片仍然放进同一次工具结果。

截图实现会把 Excel 设为可见并拉到前台（`ScreenshotCommands.cs` 里 `Visible = true` 和 `BringExcelToForeground`）。每次写操作都会短暂抢走当前桌面的焦点。这是核对的代价，不另做一套不激活窗口的截图。

截图或重算失败（锁屏、无桌面、对话框、超时）：文件保持已保存的状态，`committed` 保持 true，`verified: false`，`ok: false`。`suggestedNextActions` 只给 `verify`，不把原来的 `ops` 再发一遍。

### 提交和重试

COM 路径在 `session close --save` 之前都算未提交。`--stop-on-error` 停在失败的那一步时，关闭不保存，磁盘上的文件与调用前相同。实现时要核对每个 action 是否自己调用了 `Save`；若某条命令中途保存，就不能靠丢弃会话回滚，该 action 必须标成立即落盘，失败返回里写明 `committed: true` 和已完成的前缀。

openpyxl 路径在原子替换成功之前不算提交。替换过程中失败则留下临时文件路径，不把半截文件当成工作簿。

`verify` 是 `ops` 里的一个动作，不是第四个 MCP 工具。它只打开、按上次规则重算、保存缓存、按给定范围截图、关闭。用于 `committed: true` 且 `verified: false` 的后续调用。

筛选状态下插行、合并单元格或数组公式压在插入点、工作表保护，COM 会失败。这类失败发生在保存前，按未提交返回。

## 锁

锁文件放在系统临时目录，不放在工作簿旁边，避免只读目录和同步盘扫到它。键是 `Path.GetFullPath` 之后的绝对路径，大小写按 Windows 折叠。内容包含 PID、进程启动时间、心跳时间、工作簿路径。

- 创建用独占创建，避免两个进程同时认为自己拿到锁。
- 等待 COM 和截图时心跳照常更新。
- 只有 PID 已死，且启动时间与锁里记录的不一致或进程已不存在，才允许接管。心跳过期但进程还活着，不接管。
- 释放锁发生在本次工作簿 `Close` 成功之后。COM 超时时先结束这次认到的 Excel PID，确认文件不再被它占用，再解锁。认不出 PID 时不 Force-kill，返回 `excel_busy`，锁保留到进程真正退出。

用户自己打开的 Excel 不算我们的进程。独占探测失败就停止，不附着过去读未保存的内容。

## 给编程助手的三个工具

模型不传 `engine`，不传 `session_id`。每次带工作簿绝对路径。

| 工具 | 行为 |
|---|---|
| `workbook_read` | 值、公式、缓存值。`includeStyles` 的形状与写入相同。默认最多 4000 格，返回 `nextRange`。拿路径锁。只读，不截图。 |
| `workbook_apply` | `ops`：值、公式、LAMBDA 名称、排版、新建工作表，以及会触发 COM 的结构操作。服务端整批路由、保存、核对。 |
| `excel_exec` | 同一份 `ops`，外加 VBA、表、透视表、图表、Power Query、数据模型。末尾仍是同一次核对。 |

`workbook_apply` 不因为模型选了这个名字就强制走 openpyxl。路由只看 ops 和 zip 部件。

阶段 2 的排版不含条件格式、数据验证、批注、超链接、冻结窗格、隐藏工作表。这些在 excelcli 里已有命令，阶段 4 按 action 映射进来，映射之前调用直接返回 `unsupported`，不落到 openpyxl。

单次写入超过 10 万格返回 `too_large`，不截断保存。

返回形状：

```json
{
  "ok": false,
  "committed": true,
  "saved": true,
  "verified": false,
  "calculated": true,
  "calc": "done",
  "engine": "com",
  "path": "D:/books/a.xlsx",
  "completedOps": 2,
  "failedAt": null,
  "wrote": [{"sheet": "参数", "range": "C3:C4"}],
  "screenshots": [
    {"sheet": "参数", "range": "C3:C12", "cropped": false, "spillUnknown": false}
  ],
  "omitted": [{"sheet": "汇总", "reason": "dependents exceed capture budget"}],
  "error": {"code": "verify_failed", "message": "screenshot timed out"},
  "suggestedNextActions": [{"tool": "workbook_apply", "ops": [{"action": "verify", "sheets": ["参数"]}]}]
}
```

`engine` 只说明这次实际走了哪边。`ok` 仅在 `committed` 与 `verified` 都为 true，且需要重算时 `calculated` 为 true 时为 true。失败给 `error.code`，不返回大段 COM 堆栈。

同一文件的并行调用在锁上排队。工具描述要求模型把同一文件的修改合成一次 `ops`。`committed: true` 之后禁止把同一批结构操作原样重发。

Skill 里写清：数字格式用 Excel 格式码；桌面上已经打开的同一文件要先保存并关闭；公式用英文函数名；VBA 需要信任对工程对象模型的访问，且只有运行 VBA 的那次调用才放开宏；截图由服务端执行，不要再单独要图；表体不要用区域格式刷；`verify` 用来补核对，不用来再插入一遍。

## 做表规范

金融、分析、游戏或任何还没点名的行业，结构不一样，颜色不跟着行业走。调用方按列的职责拼块，服务端把 `layout` 展开成现有的 `ops`。`profile` 只说明这类工作通常先放哪些块，不改变字色，也不禁止多出来的块。

共用标记：

| 层级 | 字色 | 字号 | 底色 |
|---|---|---|---|
| L0 表名 | `#1F4E79` 粗体 | 16 | `#F2F2F2`，铺开本表用到的列 |
| L1 节 | `#1F4E79` 粗体 | 12 | `#E7E6E6`，铺开这一组的列 |
| L2 子节 | `#404040` 粗体 | 10 | `#F2F2F2`，只铺标题格 |
| L3 列头 | `#404040` 粗体 | 10 | `#D9D9D9`，左对齐、垂直居中 |
| 数据 | 见下 | 10 | `#FFFFFF` |
| 可录入 | `#0000FF` | 10 | `#F3F8FC`，细边 `#D8E6F0` |

字体是微软雅黑。空值或 `—` 用 `#808080`。公式含 `[` 用 `#FF0000`，含 `!` 用 `#008000`，同一张表里的引用用 `#000000`。序号和 ID 用 `0`，百分数用 `0.00%`，日期用 `yyyy-mm-dd`，其余数字用 `0.000000;-0.000000;0.000000`。

块只有这些：

| 块 | 用途 | 规则 |
|---|---|---|
| `title` / `section` / `subsection` | 表名和分组 | 只做标题，不放数据 |
| `parameters` | 假设、口径、可调项 | 录入格蓝字淡底；写成公式的格按公式上色，不再当录入 |
| `catalog` | 一行一条的清单 | `group` 列变化时，新组首行加细黑上边线 |
| `formula` | 勾稽、测算 | `bands` 横向排列，带与带之间空一列，各带自己的 L1 底色 |
| `fact` | 明细事实 | 建成 Excel 表。表头和表体不发 `format`；加行用 `table_append` |

建议组合，不是边界：

- `finance`：表名、节、参数、公式、目录。测算、附注、科目用这个起稿。
- `analytics`：表名、节、参数、事实表、目录。明细、口径、维度用这个起稿。
- `general`：上面全部块。游戏、制造、项目或没有点名的行业用这个，按列挑选。

改已经存在的表时，先认出块，再改块里面的值。已经是 Excel 表就当事实表，有蓝字淡底就当参数，一行一条就当目录，空列隔开的公式区当关系。不因为这次的主题把整张表刷成另一个行业的样子。

`layout` 在进入路由之前展开。事实表会让整批走 COM；只有参数、目录和公式的表仍走 openpyxl。

## 目录

```text
excel-sovereign-mcp/
  开发方案.md
  vendor/excel-mcp-server/     已克隆，openpyxl 来源，浅克隆 f51340e
  vendor/mcp-server-excel/     COM 与截图来源，浅克隆 cea4c31
  src/excel_sovereign/
    server.py       三个工具
    lock.py         跨进程路径锁
    inspect.py      zip 部件与表 ref 扫描
    book.py         纯包的排版与写入，keep_vba，一次保存
    excel_cli.py    ops 收成 excelcli batch，固定 --stop-on-error
    verify.py       重算、溢出范围、截图、COM 总超时
    route.py        整批走 openpyxl 或升到 COM
    layout.py       做表标记与块，展开 layout
  skills/excel-sovereign/SKILL.md
```

`book.py` 是唯一调用 openpyxl 的地方。`excel_cli.py` 是唯一调用 excelcli 的地方。

## 分期

1. 确认两个 vendor 克隆完整。装好或编译出 `excelcli`。对一个样例文件跑通 `session open` → `screenshot capture` → `session close`，并确认关闭后文件不再被 Excel 占用。
2. `workbook_read`、`workbook_apply`（纯包操作），写完必截图。验收：两文件并行写、同文件连续写、只改字色也会出图且 `calc` 为 `skipped`、纯 xlsm 往返后 `vbaProject.bin` 还在、读请求会等到写锁释放。
3. 结构操作和重算走 COM，末尾仍是同一次核对。验收：重命名后跨表公式和定义名称指向新表名；插行后跨表公式和 LAMBDA 名称仍指向原单元格；改一个输入常数后，另一张表的缓存值已更新；动态数组溢出区进入截图范围；失败发生在保存前时文件字节不变；截图失败后再调 `verify` 不会把行再插入一次。
4. 按使用顺序映射 excelcli 已有 action：Excel 表（含与表相交的写入）、透视表、图表，然后 Power Query，然后数据模型和 DAX，然后条件格式、数据验证、批注。每加一个 action 只改映射和 Skill。某个 action 的参数大到挤占上下文时，再单独拆工具。

阶段 2 另备一份含透视表或 Power Query 的样例：只改一个字色，必须走 COM，包内对应部件还在。阶段 3 用筛选表、保护表、合并单元格压住插入点各做一次失败路径，确认 `committed: false`。

## 不做的事

不把 sbroenne 的 31 个 MCP 工具原样挂进 Cursor。不让模型管理 `session_id`，也不让模型记得自己去截图。不在 openpyxl 或 ExcelJS 里补公式依赖平移。不把 haris-musa 的 `pivot.py` 当成透视表。不使用 knorq 那种只在单个进程里生效、并且读不加的锁。不把每个文件做成一个 Excel 进程，也不附着到用户已经打开的 Excel。不在 openpyxl 路径上保存含透视表、查询、数据模型、切片器、图表或 rich data 的工作簿。
